SQL Basics
In this minor project the following steps are I followed
STEP-1: create the data base with proper commenting lines
STEP-2: create the table name with field names and defined with suitable data types
STEP-3: From the above I use DDL(Data Definition Language) commands which is create
SPEP-4: Then insert the data values into the table by row wise which is also called tuples or records by using proper data types and insert DDL command
STEP-5: use select * to display the table data 
Here, In this data insertion I use PRIMARY KEY constraint to enter the unique data 
